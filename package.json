{
  "name": "roastlab",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"cd backend && php artisan serve\""
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  }
}
